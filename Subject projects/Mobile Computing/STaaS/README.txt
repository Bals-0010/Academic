Task:  Storage as a service (Staas)

Small GUI program to connect dropbox API, to rename, delete, add files 
and save logs in cache in case of connection issues
if connected online again the saved logs like #del file, #add file etc must be executed